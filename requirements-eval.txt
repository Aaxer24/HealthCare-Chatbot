# Extra dependencies for evaluate_chatbot.py (RAGAS-based RAG evaluation).
# Install with: pip install -r requirements.txt -r requirements-eval.txt
ragas>=0.4.3
datasets>=5.0.0
# pinned exactly -- a floating version let CI resolve a different mlflow patch than local dev and broke a run
mlflow==3.15.2
